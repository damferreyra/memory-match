---
phase: 03-grid
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/game/config/cards.ts
  - src/game/config/grid.ts
  - src/game/config/rounds.ts
  - src/game/config/ui.ts
  - src/game/game-logic.ts
  - src/game/game-logic.test.ts
  - src/game/config/grid.test.ts
autonomous: true
requirements:
  - GRID-01
  - GRID-02
  - GRID-03
  - GRID-04

must_haves:
  truths:
    - "npm run test passes with zero failures for all game-logic and grid tests"
    - "generateCardPairs(8) returns exactly 16 items with each symbolId 0-7 appearing twice"
    - "getCardPosition(index, GRID_LAYOUT) returns pixel coords within the 1024x768 canvas for all 16 indices"
    - "All constants in config files match MEMORY_GAME_PLAN.md verbatim — no magic numbers in game code"
    - "game-logic.ts has zero Phaser imports (Vitest can run it without mocking)"
  artifacts:
    - path: "src/game/config/cards.ts"
      provides: "CARD_WIDTH, CARD_HEIGHT, CARD_CORNER_RADIUS, CARD_BACK_COLOR, CARD_BACK_BORDER, CARD_MATCHED_TINT, SYMBOLS, Symbol type, SYMBOL_COLORS, SYMBOL_FONT_SIZE, CARD_FACE_BG, CARD_FACE_BORDER"
      min_lines: 20
    - path: "src/game/config/grid.ts"
      provides: "GRID_COLS, GRID_ROWS, GRID_GAP, GRID_ORIGIN_X, GRID_ORIGIN_Y, GridLayout interface, GRID_LAYOUT, getCardPosition()"
      min_lines: 30
    - path: "src/game/config/rounds.ts"
      provides: "RoundConfig interface, ROUND_CONFIGS array (3 entries)"
      min_lines: 10
    - path: "src/game/config/ui.ts"
      provides: "CARD_DEPTH, HUD_DEPTH, OVERLAY_DEPTH, HUD_HEIGHT, TIMER_BAR_WIDTH, TIMER_BAR_HEIGHT, SCORE_FONT_SIZE, TIMER_COLOR_NORMAL, TIMER_COLOR_URGENT, TIMER_URGENT_THRESHOLD, OVERLAY_PANEL_WIDTH, OVERLAY_PANEL_HEIGHT, OVERLAY_BACKDROP_ALPHA, FLIP_DURATION_MS, MISMATCH_HOLD_MS, STREAK_BONUS_PER_MATCH, STREAK_BONUS_MAX, BASE_MATCH_SCORE, TIME_BONUS_PER_SECOND"
      min_lines: 25
    - path: "src/game/game-logic.ts"
      provides: "CardState type, CardData interface, shuffle<T>(), generateCardPairs(), isMatch(), computeMatchScore(), computeTimeBonus()"
      exports: ["CardState", "CardData", "shuffle", "generateCardPairs", "isMatch", "computeMatchScore", "computeTimeBonus"]
      min_lines: 35
    - path: "src/game/game-logic.test.ts"
      provides: "Vitest tests covering shuffle, generateCardPairs, isMatch, computeMatchScore, computeTimeBonus"
      min_lines: 40
    - path: "src/game/config/grid.test.ts"
      provides: "Vitest tests covering getCardPosition for indices 0 and 15, all 16 distinct positions, canvas bounds"
      min_lines: 25
  key_links:
    - from: "src/game/game-logic.ts"
      to: "src/game/config/ui.ts"
      via: "import { STREAK_BONUS_PER_MATCH, STREAK_BONUS_MAX, BASE_MATCH_SCORE, TIME_BONUS_PER_SECOND }"
      pattern: "import.*config/ui"
    - from: "src/game/config/grid.ts"
      to: "src/game/constants.ts"
      via: "import { GAME_WIDTH } for GRID_ORIGIN_X computation"
      pattern: "import.*constants"
    - from: "src/game/config/grid.ts"
      to: "src/game/config/cards.ts"
      via: "import { CARD_WIDTH, CARD_HEIGHT } for GRID_LAYOUT"
      pattern: "import.*cards"
---

<objective>
Create all four config modules with exact constant values from MEMORY_GAME_PLAN.md, implement the pure game-logic module with zero Phaser imports, and write Vitest unit tests that prove the logic is correct before any rendering code exists.

Purpose: Phase 4 flip mechanics and Phase 5 scoring both import these constants and functions. Getting them right with tested, verified behavior now prevents silent breakage downstream. The "pure module" constraint (no Phaser imports in game-logic.ts) is architectural — it keeps the test suite fast and dependency-free.

Output: 7 files (4 config modules, game-logic.ts, game-logic.test.ts, config/grid.test.ts) all committed and `npm run test` fully green.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-grid/03-CONTEXT.md

<interfaces>
<!-- Existing exports the executor will import from. Extracted from codebase — no exploration needed. -->

From src/game/constants.ts:
```typescript
export const GAME_WIDTH = 1024;
export const GAME_HEIGHT = 768;

export const SCENE_KEYS = {
  BOOT: 'BootScene',
  MENU: 'MenuScene',
  GAME: 'GameScene',
  UI: 'UIScene',
} as const;

export const REGISTRY_KEYS = {
  CURRENT_ROUND: 'currentRound',
  TOTAL_SCORE: 'totalScore',
  TOTAL_ROUNDS: 'totalRounds',
} as const;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config modules (cards, grid, rounds, ui)</name>
  <files>
    src/game/config/cards.ts,
    src/game/config/grid.ts,
    src/game/config/rounds.ts,
    src/game/config/ui.ts
  </files>
  <behavior>
    - cards.ts exports CARD_WIDTH=140, CARD_HEIGHT=140, CARD_CORNER_RADIUS=12
    - cards.ts exports CARD_BACK_COLOR=0x2c3e50, CARD_BACK_BORDER=0x1a252f, CARD_MATCHED_TINT=0x888888
    - cards.ts exports SYMBOLS as const array of 8 emoji strings, Symbol type derived from it
    - cards.ts exports SYMBOL_COLORS as Record<Symbol, number> with 8 hex integer entries
    - cards.ts exports SYMBOL_FONT_SIZE=52, CARD_FACE_BG=0xffffff, CARD_FACE_BORDER=0xcccccc
    - grid.ts imports GAME_WIDTH from ../constants and CARD_WIDTH, CARD_HEIGHT from ./cards
    - grid.ts exports GRID_COLS=4, GRID_ROWS=4, GRID_GAP=12, GRID_ORIGIN_Y=90
    - grid.ts exports GRID_ORIGIN_X computed as (GAME_WIDTH - GRID_COLS * CARD_WIDTH - (GRID_COLS - 1) * GRID_GAP) / 2 = 214
    - grid.ts exports GridLayout interface, GRID_LAYOUT object, and getCardPosition() pure function
    - rounds.ts exports RoundConfig interface and ROUND_CONFIGS array: [{round:1, timeLimit:60, peekDuration:1.5}, {round:2, timeLimit:45, peekDuration:1.0}, {round:3, timeLimit:30, peekDuration:0.5}]
    - ui.ts exports depth constants: CARD_DEPTH=10, HUD_DEPTH=20, OVERLAY_DEPTH=30
    - ui.ts exports HUD_HEIGHT=70, TIMER_BAR_WIDTH=400, TIMER_BAR_HEIGHT=18, SCORE_FONT_SIZE=22
    - ui.ts exports TIMER_COLOR_NORMAL=0x27ae60, TIMER_COLOR_URGENT=0xe74c3c, TIMER_URGENT_THRESHOLD=10
    - ui.ts exports OVERLAY_PANEL_WIDTH=440, OVERLAY_PANEL_HEIGHT=280, OVERLAY_BACKDROP_ALPHA=0.65
    - ui.ts exports FLIP_DURATION_MS=150, MISMATCH_HOLD_MS=800
    - ui.ts exports STREAK_BONUS_PER_MATCH=50, STREAK_BONUS_MAX=200, BASE_MATCH_SCORE=100, TIME_BONUS_PER_SECOND=10
    - No file imports from Phaser — all plain TypeScript constants
  </behavior>
  <action>
    Create four config files with EXACT values from MEMORY_GAME_PLAN.md. Do NOT deviate from any constant value.

    src/game/config/cards.ts — Copy the schema verbatim from MEMORY_GAME_PLAN.md "config/cards.ts" section. The SYMBOLS array must use `as const`. The Symbol type must be `(typeof SYMBOLS)[number]`. SYMBOL_COLORS must be `Record<Symbol, number>` with hex integer values (0xRRGGBB format — never CSS strings).

    src/game/config/grid.ts — Import GAME_WIDTH from '../constants' and CARD_WIDTH, CARD_HEIGHT from './cards'. Compute GRID_ORIGIN_X as shown (result is 214). Export GridLayout interface, GRID_LAYOUT constant, and getCardPosition() pure function. getCardPosition signature: `(index: number, layout: GridLayout): { x: number; y: number }`. No Phaser imports.

    src/game/config/rounds.ts — Export RoundConfig interface with `round: number`, `timeLimit: number`, `peekDuration: number`. Export ROUND_CONFIGS array with exactly 3 entries.

    src/game/config/ui.ts — Export all constants listed in MEMORY_GAME_PLAN.md ui.ts schema. Colors as hex integers. No Phaser imports.

    After creating files, run `npm run lint` to confirm Biome formatting passes (tabs, single quotes). Fix any lint errors before proceeding.
  </action>
  <verify>
    <automated>cd "C:/Users/damfe/Documents/projects/lab/memory-match" && npm run lint</automated>
  </verify>
  <done>All four config files exist, `npm run lint` passes, no TypeScript errors. Constants match MEMORY_GAME_PLAN.md exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: game-logic.ts and both test files (RED then GREEN)</name>
  <files>
    src/game/game-logic.ts,
    src/game/game-logic.test.ts,
    src/game/config/grid.test.ts
  </files>
  <behavior>
    game-logic.ts:
    - Zero Phaser imports — only imports from ./config/ui (scoring constants)
    - Exports CardState type: 'faceDown' | 'flippingUp' | 'faceUp' | 'flippingDown' | 'matched'
    - Exports CardData interface: { id: number; symbolId: number; state: CardState }
    - shuffle<T>(array: T[]): T[] — Fisher-Yates in-place shuffle, mutates and returns
    - generateCardPairs(symbolCount: number): number[] — builds [0,0,1,1,...,7,7] then shuffles
    - isMatch(symbolA: number, symbolB: number): boolean — returns symbolA === symbolB
    - computeMatchScore(streakCount: number): number — caps bonus at STREAK_BONUS_MAX
    - computeTimeBonus(secondsRemaining: number): number — Math.floor(seconds) * TIME_BONUS_PER_SECOND

    game-logic.test.ts tests:
    - shuffle: result has same length; result.sort() equals sorted input
    - generateCardPairs(8): returns array of length 16
    - generateCardPairs(8): each symbolId 0-7 appears exactly twice
    - isMatch(3, 3): returns true
    - isMatch(3, 5): returns false
    - computeMatchScore(100): equals BASE_MATCH_SCORE + STREAK_BONUS_MAX (capped)
    - computeMatchScore(0): equals BASE_MATCH_SCORE (no bonus at streak 0)
    - computeTimeBonus(15): equals 150 (15 * TIME_BONUS_PER_SECOND=10)
    - computeTimeBonus(15.9): equals 150 (floor applied)

    config/grid.test.ts tests:
    - getCardPosition(0, GRID_LAYOUT): x > 0, x < GAME_WIDTH, y > 0, y < GAME_HEIGHT
    - getCardPosition(15, GRID_LAYOUT): x < GAME_WIDTH, y < GAME_HEIGHT
    - All 16 positions (indices 0-15) are distinct (unique x,y pairs)
    - No position has x > GAME_WIDTH or y > GAME_HEIGHT
  </behavior>
  <action>
    TDD cycle — RED then GREEN:

    RED: Write game-logic.test.ts and config/grid.test.ts first with all tests from the behavior block above. Use this import structure for game-logic.test.ts:
    ```typescript
    import { describe, expect, it } from 'vitest';
    import { generateCardPairs, isMatch, computeMatchScore, computeTimeBonus, shuffle } from './game-logic';
    import { BASE_MATCH_SCORE, STREAK_BONUS_MAX, TIME_BONUS_PER_SECOND } from './config/ui';
    ```
    And for config/grid.test.ts:
    ```typescript
    import { describe, expect, it } from 'vitest';
    import { getCardPosition, GRID_LAYOUT } from './grid';
    import { GAME_WIDTH, GAME_HEIGHT } from '../constants';
    ```
    Run `npm run test` — tests must fail because game-logic.ts does not exist yet.

    GREEN: Write src/game/game-logic.ts implementing all exports. Import only from './config/ui'. Use Fisher-Yates shuffle:
    ```typescript
    export function shuffle<T>(array: T[]): T[] {
      for (let i = array.length - 1; i > 0; i--) {
        const j = Math.floor(Math.random() * (i + 1));
        [array[i], array[j]] = [array[j], array[i]];
      }
      return array;
    }
    ```
    generateCardPairs: `Array.from({ length: symbolCount }, (_, i) => [i, i]).flat()` then shuffle.
    computeMatchScore: `const bonus = Math.min(streakCount * STREAK_BONUS_PER_MATCH, STREAK_BONUS_MAX); return BASE_MATCH_SCORE + bonus;`
    computeTimeBonus: `return Math.floor(secondsRemaining) * TIME_BONUS_PER_SECOND;`

    Run `npm run test` — all tests must pass.

    CRITICAL: After GREEN, verify with `grep -r "phaser" src/game/game-logic.ts` returns nothing. If any Phaser import slipped in, remove it. Vitest runs in Node (no DOM/canvas) — any Phaser import will fail the entire test suite.

    Also verify TypeScript: all parameters in every exported function must be used (noUnusedParameters is strict). The `streakCount` param in computeMatchScore must be referenced.
  </action>
  <verify>
    <automated>cd "C:/Users/damfe/Documents/projects/lab/memory-match" && npm run test</automated>
  </verify>
  <done>npm run test passes with zero failures. Both test files exist. game-logic.ts has zero Phaser imports. All exports match the behavior block exactly.</done>
</task>

</tasks>

<verification>
Full phase check before marking plan complete:

1. `npm run test` — all tests green (game-logic.test.ts + config/grid.test.ts)
2. `npm run lint` — Biome passes (tabs, single quotes, no unused locals)
3. `grep -r "phaser" src/game/game-logic.ts` — returns nothing (zero Phaser imports)
4. Verify GRID_ORIGIN_X in config/grid.ts equals 214 (computed, not hardcoded)
5. Verify SYMBOLS array has exactly 8 entries matching: `['★', '♦', '♠', '♥', '✿', '♪', '▲', '●']`
6. Verify ROUND_CONFIGS has exactly 3 entries with correct timeLimit/peekDuration values
</verification>

<success_criteria>
- `npm run test` exits 0 with all tests passing
- `npm run lint` exits 0
- 7 new files committed: config/cards.ts, config/grid.ts, config/rounds.ts, config/ui.ts, game-logic.ts, game-logic.test.ts, config/grid.test.ts
- No constant value deviates from MEMORY_GAME_PLAN.md
- game-logic.ts imports nothing from Phaser
</success_criteria>

<output>
After completion, create `.planning/phases/03-grid/03-01-SUMMARY.md` documenting:
- Files created and their key exports
- Test results (counts passing)
- Any deviations from the plan (there should be none)
- Constants verified against MEMORY_GAME_PLAN.md
</output>
